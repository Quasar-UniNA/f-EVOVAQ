version: "2"

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.11"

python:
  install:
    - method_ pip
      path: .
    - requirements: requirements.txt

sphinx:
  configuration: docs/source/conf.py
